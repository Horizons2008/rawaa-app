import 'package:flutter/material.dart';

class DetailCategorie extends StatelessWidget {
  const DetailCategorie({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}